transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+F:/FPGA\ Project/ep5/stopwatch {F:/FPGA Project/ep5/stopwatch/stopwatch.v}
vlog -vlog01compat -work work +incdir+F:/FPGA\ Project/ep5/stopwatch {F:/FPGA Project/ep5/stopwatch/ep5_1.v}

